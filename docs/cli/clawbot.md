---
summary: "CLI reference for `shittimchest clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `shittimchest clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `shittimchest clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `shittimchest clawbot qr` (same behavior as [`shittimchest qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `shittimchest clawbot qr` -> `shittimchest qr`
