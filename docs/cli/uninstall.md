---
summary: "CLI reference for `shittimchest uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `shittimchest uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
shittimchest uninstall
shittimchest uninstall --all --yes
shittimchest uninstall --dry-run
```
